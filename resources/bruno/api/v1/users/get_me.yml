info:
  name: get_me
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
