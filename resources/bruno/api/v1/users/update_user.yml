info:
  name: update_user
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/api/v1/users/cpSYV6jO3OJxSWnwSGQs0"
  body:
    type: json
    data: |-
      {
        "address": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
