info:
  name: list_categories
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/products/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
