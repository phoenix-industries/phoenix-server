info:
  name: list_products
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
