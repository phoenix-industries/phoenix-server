info:
  name: delete_product
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/products/12123"
  headers:
    - name: Authorization
      value: "{{TOKEN_TYPE}} {{ACCESS_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
