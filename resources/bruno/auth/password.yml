info:
  name: password
  type: http
  seq: 5

http:
  method: PUT
  url: "{{BASE_URL}}/auth/password"
  body:
    type: json
    data: |-
      {
        "password": "lOl@$1212",
        "new_password": "lOl@$12121"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
